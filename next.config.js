/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/Navigator_Dashboard_NextJS',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
